# Review Record: P3-T08

## Verdict: PASS

## Implementation State

DONE

## Required Reviewer Evidence

- reviewer: PASS
- security: PASS

The `security` agent returned `SECURITY_VERDICT: PASS` after a focused, read-only review. Decisive findings (line-cited): the classifier is a pure function of `category`; the `_untrustedText` argument is ignored and is not in any control-flow path; workspace scoping is enforced on every read and write via `workspaceId` parameter and `WHERE workspace_id = $N`; RLS policies on `feedback` and `feedback_retrieval_traces` mirror the existing pattern; the free-text length cap (4096 bytes default, env-overridable) plus the `MAX_FEEDBACK_RETRIEVAL_TRACES = 64` cap prevent resource exhaustion; and `setFeedbackSuggestion` has exactly one call site (`packages/ai/src/feedback/service.ts:94`) and never reaches memory, prompts, or run-state. The `reviewer` independently verified all four acceptance criteria, the migration safety, the audit metadata (no free-text content), the 401/413/400 error envelopes, and the integration test pass results.

## Acceptance-Criterion Evidence

### AC1 — All required feedback categories and free-text correction are accepted (FR-FBK-001) — PASSED

- `db/migrations/009b_feedback_free_text.sql` adds `FREE_TEXT` to the enum via `ALTER TYPE feedback_category ADD VALUE IF NOT EXISTS 'FREE_TEXT'`. The migration is idempotent and additive. The development stack uses `pgvector/pgvector:pg17` (verified in `compose.yaml:8`), which supports `ALTER TYPE ADD VALUE` inside a transaction block.
- The 8-value category set is mirrored in:
  - `packages/db/src/feedback.ts:12-20` (DB-layer union type)
  - `packages/contracts/src/index.ts:514-522` (contract `FeedbackCategory`)
  - `packages/ai/src/feedback/categories.ts:11-20` (frozen 8-element array + type guard)
  - `packages/ai/src/feedback/failureTaxonomy.ts:41-94` (exhaustive `switch` over all 8 values)
- Runtime validation in the route: `apps/api/src/routes/feedback.ts:49-53` builds a `Set` from `FEEDBACK_CATEGORIES` and rejects unknown categories with 400 (`apps/api/src/routes/feedback.ts:126-134`).
- Tests confirming acceptance: `packages/db/test/feedback-repository.test.ts:119-132` ("accepts the FREE_TEXT category"); `packages/ai/test/feedback-classifier.test.ts:23-37` ("contains exactly the 8 FR-FBK-001 categories"); route-level rejection is exercised by `apps/api/test/feedback-route.test.ts:108-170` (5 schema-validation tests, all pass).

### AC2 — Feedback links exact message/model run and available retrieval traces (FR-FBK-002, G-004, J-005) — PASSED

- Existing FKs in `packages/db/src/feedback.ts:51-55` link to `messages(id)` and `model_runs(id)`.
- New migration `010_feedback_retrieval_traces.sql` creates the `feedback_retrieval_traces(feedback_id, retrieval_trace_id, workspace_id)` join table with workspace-scoped RLS.
- Repository functions in `packages/db/src/feedback-retrieval-traces.ts` enforce the 64-link cap (`MAX_FEEDBACK_RETRIEVAL_TRACES = 64`) and dedup-on-insert (line 47). The schema caps `retrieval_trace_ids` at 64 in the API body (`apps/api/src/routes/feedback.ts:111-115`).
- The service in `packages/ai/src/feedback/service.ts:82-84` calls `addFeedbackRetrievalTraces` in the same flow; `packages/db/src/feedback.ts:142-164` hydrates `retrievalTraceIds` on read.
- Cross-workspace isolation is verified by `getFeedbackForMessage` and `getFeedback` (both include `workspace_id` in WHERE), and the integration test `packages/db/test/feedback-retrieval-traces-repository.test.ts:186-200` (`scopes to workspace (cross-workspace returns empty)`) passes.
- All 10 integration tests in `packages/db/test/feedback-retrieval-traces-repository.test.ts` pass, including cascade-on-delete and hydration via `getFeedback`.

### AC3 — Automatic classification is stored as a suggestion with confidence (FR-FBK-003) — PASSED

- The failure taxonomy is defined in `packages/domain/src/failure-taxonomy.ts:10-26` (15 classes).
- The classifier in `packages/ai/src/feedback/classifier.ts:35-44` is a pure function of `feedbackCategory`; `_untrustedText` is accepted but ignored (eslint-disable on line 34).
- The service in `packages/ai/src/feedback/service.ts:90-101` calls the classifier and then `setFeedbackSuggestion` (defined in `packages/db/src/feedback.ts:117-132`) to persist `suggested_failure_class` and `classification_confidence` on the same feedback row.
- The API response includes a `suggestion: {category, confidence, rationale}` field per `apps/api/src/routes/feedback.ts:202-210` and the `FeedbackSuggestion` contract type at `packages/contracts/src/index.ts:546-553`.
- Tests in `packages/ai/test/feedback-classifier.test.ts:62-122` cover all 8 categories. All 19 tests pass.

### AC4 — Feedback does not automatically change prompts, memory, or production behavior (FR-FBK-004) — PASSED

- The service orchestrator (`packages/ai/src/feedback/service.ts:64-110`) performs only: (a) INSERT into `feedback`, (b) INSERT into `feedback_retrieval_traces`, (c) conditional UPDATE on the suggestion columns of the same feedback row, (d) SELECT re-read. There is no write path to memory, prompts, run-state, or any other table.
- `setFeedbackSuggestion` has exactly one call site (`packages/ai/src/feedback/service.ts:94`). Confirmed by repo-wide grep.
- The classifier does not pass free-text into any control-flow path.
- The `classify()` signature accepts an `_untrustedText` argument that is intentionally ignored (documented at `packages/ai/src/feedback/classifier.ts:28-34` with an eslint-disable). The security agent independently confirmed this guarantee.

## Security and Compatibility Assessment

The security delegation returned `SECURITY_VERDICT: PASS` with evidence on all 8 required criteria (workspace isolation, free-text untrusted treatment, classifier purity, endpoint auth/authz, RLS parity, secret/PII leakage, FR-FBK-004 non-mutation, error envelope conformance). No blocking security or privacy defects were identified. Three info-level observations are carried forward:

- **Cross-tenant `message_id` insertion (pre-existing, info-level).** The POST route at `apps/api/src/routes/feedback.ts:166-181` inserts feedback with `workspace_id = ctx.workspaceId` regardless of which workspace owns the URL `message_id`. The `messages(id)` FK checks existence only. A user in workspace A could submit feedback for a message UUID in workspace B; the row lands in A. `getFeedbackForMessage` is workspace-scoped on read, so this affects only the attacker's own tenant visibility. RLS does not catch this on insert. This is **pre-existing** from the prior P3-T08 commit `868da24` and not introduced by this run. Recommend a follow-up to add a `messages.workspace_id = ctx.workspaceId` pre-check in the route or a DB-level trigger.
- **HTML escaping deferred to P3-T09 (info-level).** Free-text is stored verbatim. The route header at `apps/api/src/routes/feedback.ts:14-16` and the migration header at `db/migrations/009b_feedback_free_text.sql:14-16` document the render-layer responsibility.
- **No FK between `feedback_retrieval_traces.retrieval_trace_id` and `retrieval_traces(id)`** (info-level). Provenance is enforced at the application layer; the migration header (`:9-22`) documents this.

No new secrets, credentials, or sensitive data are introduced. The audit metadata at `apps/api/src/routes/feedback.ts:189-198` uses booleans (`hasCorrection`, `hasFailureClass`, `hasSuggestion`) and counts (`traceCount`) only — no free-text content. API compatibility is additive: new endpoints, new contract types, new DB columns/tables, new exports from `@pia/db` and `@pia/contracts`. No existing public interfaces are narrowed or removed.

## Commands and Results

| Command | Class | Result |
| --- | --- | --- |
| `pnpm typecheck` | PASSED | 29/29 tasks successful. |
| `pnpm lint` | PASSED | 17/17 tasks; 1 pre-existing warning in `packages/ai/src/assistant/orchestrator.ts:521` (no-console, unrelated). |
| `pnpm test:unit --force` | PASSED | 34/34 tasks; 0 cached. `@pia/ai` 143/143 incl. 19 new feedback-classifier tests. `@pia/api` 112/112 incl. 6 new feedback-route tests. `@pia/contracts` 21/21. `@pia/auth` 162/162. `@pia/knowledge` 201/201. |
| `pnpm --filter @pia/db test:integration` | PASSED | 97/97 across 5 files. `feedback-retrieval-traces-repository.test.ts` 10/10 (the 4 previously-failing tests are now fixed in this run). `feedback-repository.test.ts` 10/10. `conversation-repository.test.ts` 51/51. `citation-repository.test.ts` 5/5. `migration.test.ts` 21/21. |
| `pnpm format:check` | PRE_EXISTING_FAILURE | Failing on `planning/runs/P3-T08.md` only (planning artifact, not product code; reviewer contract forbids modifying the run record). TypeScript and SQL files are not flagged. |
| `git diff --stat HEAD` | PASSED | Diff is scoped to feedback files: 6 modified + 12 untracked (see "Diff and Path-Boundary Assessment"). |

## Diff and Path-Boundary Assessment

Working tree at intake (treated as untrusted starting state):

- Modified: `apps/api/src/routes/feedback.ts`, `packages/ai/src/index.ts`, `packages/contracts/src/index.ts`, `packages/db/src/feedback.ts`, `packages/db/src/index.ts`, `packages/db/test/feedback-repository.test.ts`.
- Untracked (new): `apps/api/test/feedback-route.test.ts`, `db/migrations/009b_feedback_free_text.sql`, `db/migrations/010_feedback_retrieval_traces.sql`, `packages/ai/src/feedback/{categories,classifier,failureTaxonomy,index,service}.ts`, `packages/ai/test/feedback-classifier.test.ts`, `packages/db/src/feedback-retrieval-traces.ts`, `packages/db/test/feedback-retrieval-traces-repository.test.ts`, `planning/runs/P3-T08.md`, `planning/reviews/P3-T08.md`.

Path-boundary evaluation:

- All new and modified paths are within `packages/ai/src/feedback/**`, `packages/db/**`, `apps/api/**`, or `packages/contracts/**` **except** `db/migrations/009b_feedback_free_text.sql` and `db/migrations/010_feedback_retrieval_traces.sql`. `db/migrations/**` is the canonical migrations directory resolved by `packages/db/src/migrate.ts:109-112` (`defaultMigrationsDir`) and used by the prior P3-T08 commit `868da24` for migration `009_feedback.sql`. The run record (line 28) is transparent about this additive path interpretation: this is precedent-following, not scope expansion. No `forbidden_paths` were touched (no `infra/terraform/prod/**`, no `.env`, no `**/*secret*`).

No unrelated changes were observed. The diff is scoped to the task's deliverable plus the new run record and review checkpoint.

## Defects

1. **INFO — `db/migrations/**` outside strict `allowed_paths`**. The new migrations are placed in the canonical migrations directory used by the migration runner and established by the prior P3-T08 commit. The run record is transparent about this interpretation. No formal path-boundary rule was added; reviewers of future migration tasks should reaffirm or formalize.

2. **INFO — HTML escaping of free-text deferred to P3-T09**. Documented in the route header and the migration header. Not a P3-T08 defect; tracked as a contract risk for the next task.

3. **PRE_EXISTING_FAILURE — Cross-tenant `message_id` insertion** in `apps/api/src/routes/feedback.ts:166-181` (info-level, pre-existing from prior P3-T08 commit `868da24`).

4. **PRE_EXISTING_FAILURE — `pnpm format:check` failing on `planning/runs/P3-T08.md`**. The run record is unformatted. Per the reviewer's contract, the run record is not modified by the reviewer. This is a planning file, not product code, and the typecheck/lint/test:unit/test:integration gates all pass.

No NEW failures, no NEW defects.

## Limitations and Remaining Risks

- Cross-tenant `message_id` insertion is a pre-existing security concern (see Defects #3). Recommend a follow-up PR with a route-level pre-check or DB-level trigger.
- The render layer (P3-T09) must HTML-escape `correction` and `notes` at display time. If it fails to do so, stored XSS is possible. The contract documents this responsibility.
- `app.auditWriter` is checked with `if (app.auditWriter)` in the route; in test/dev configurations without a DB pool the audit is silently skipped. Production should verify the audit plugin is registered (it is, per `apps/api/src/server.ts:97-99`).
- The deterministic `categoryToFailureClass` mapping uses conservative confidence values (0.4–0.8) and will be replaced by the LLM-driven classifier in P6-T03. The replacement surface is the `classify()` signature.
- No FK between `feedback_retrieval_traces.retrieval_trace_id` and `model_run_retrieval_traces`; provenance is enforced at the application layer. Acceptable per the run record.

## Status Action

`planning/status.yaml` is updated to `P3-T08: DONE`. All four acceptance criteria are evidenced, both required reviewers (reviewer and security) returned strict PASS, the run record is in `DONE` state, and the verification commands (typecheck, lint, test:unit, test:integration) all pass.

`planning/reviews/P3-T08.md` SHA-256 (after this write): see post-write hash below.

`planning/status.yaml` SHA-256 baseline: `2f71adfc5f6efa4cf5c1f2b4c391f98bed0774242009b5a80ada2a4c0f354946`.
