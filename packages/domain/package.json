{
  "name": "@pia/domain",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test:unit": "echo 'domain: no tests yet' && exit 0"
  }
}
