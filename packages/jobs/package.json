{
  "name": "@pia/jobs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test:unit": "echo 'jobs: no tests yet' && exit 0"
  }
}
